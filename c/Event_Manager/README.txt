Event Manager program to register the module and call the
function of every module when the registed event is published

To compile the event manager type "make" in the terminal
Give the event you want to publish as the key input
You will see the Modules registered to the event

To run the event manager run the following command in the terminal

./Event_Manager
